import React from 'react';
import MainView from "../ui/view/MainView"
const MainContainer = () => {
    return (
        <div>
           <MainView/>
        </div>
    );
};

export default MainContainer;